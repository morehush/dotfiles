#!/bin/bash

dcomp=/home/morehush/tkhtpc

rsync -turPh /home/morehush/git/dockersrv/tkhtpc/ $dcomp/

cd $dcomp
docker-compose pull
docker-compose up -d --remove-orphans
docker ps --format '{{.Names}}'
docker image prune -f
