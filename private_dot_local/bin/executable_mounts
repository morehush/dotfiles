#!/bin/bash

auth="-o credentials=$HOME/.creds,uid=1000"

mkdir -p $HOME/remote/others $HOME/remote/tv $HOME/remote/movies

sudo mount -t cifs //omv/others $HOME/remote/others $auth
sudo mount -t cifs //omv/tv $HOME/remote/tv $auth
sudo mount -t cifs //omv/movies $HOME/remote/movies $auth
