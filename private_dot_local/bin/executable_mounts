#!/bin/bash

auth="-o credentials=$HOME/.creds,uid=1000"

mkdir -p $HOME/remote/others $HOME/remote/tv $HOME/remote/movies

mount -t cifs //omv/others $HOME/remote/others $auth
mount -t cifs //omv/tv $HOME/remote/tv $auth
mount -t cifs //omv/movies $HOME/remote/movies $auth
