#!/bin/bash

auth="-o credentials=$HOME/.creds,uid=1000"

mkdir -p $HOME/remote/others $HOME/remote/tv $HOME/remote/movies

sudo mount -t cifs $auth //omv/others $HOME/remote/others
sudo mount -t cifs $auth //omv/tv $HOME/remote/tv
sudo mount -t cifs $auth //omv/movies $HOME/remote/movies
